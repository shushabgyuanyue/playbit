{
  "name": "playbit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel --filter @playbit/api --filter @playbit/web dev",
    "dev:web": "pnpm --filter @playbit/web dev",
    "dev:api": "pnpm --filter @playbit/api dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm --filter @playbit/game-core test && pnpm --filter @playbit/api test",
    "db:generate": "pnpm --filter @playbit/api db:generate",
    "db:migrate": "pnpm --filter @playbit/api db:migrate"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
